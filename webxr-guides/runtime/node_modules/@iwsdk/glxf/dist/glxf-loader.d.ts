/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Group, Loader, LoadingManager, Object3D } from 'three';
import { GLTF } from 'three/examples/jsm/loaders/GLTFLoader.js';
interface GLTFLoaderLike {
    loadAsync(url: string): Promise<GLTF>;
}
export interface GLXFAsset {
    uri: string;
    name?: string;
}
export interface GLXFNode {
    name?: string;
    asset?: number;
    translation?: [number, number, number];
    rotation?: [number, number, number, number];
    scale?: [number, number, number];
    children?: number[];
    extras?: {
        meta_spatial?: {
            entity_id?: string;
            components?: Record<string, any>;
            version?: number;
        };
    };
}
export interface GLXFScene {
    nodes: number[];
}
export interface GLXFData {
    assets: GLXFAsset[];
    nodes: GLXFNode[];
    scenes: GLXFScene[];
    scene?: number;
    asset: {
        minVersion: string;
        version: string;
    };
}
export interface GLXF {
    assets: GLTF[];
    nodes: Object3D[];
    scenes: Group[];
    scene: Group;
}
export declare class GLXFLoader extends Loader<GLXF> {
    private gltfLoader;
    constructor(manager?: LoadingManager);
    load(url: string, onLoad: (glxf: GLXF) => void, onProgress?: (event: ProgressEvent) => void, onError?: (event: ErrorEvent) => void): void;
    loadAsync(url: string, onProgress?: (event: ProgressEvent) => void): Promise<GLXF>;
    parse(text: string, path: string, onLoad: (glxf: GLXF) => void, onError?: (event: ErrorEvent) => void): void;
    parseAsync(text: string, path: string): Promise<GLXF>;
    setGLTFLoader(gltfLoader: GLTFLoaderLike): this;
    private createNodesAndScenesFromGLXF;
}
export {};
//# sourceMappingURL=glxf-loader.d.ts.map