/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Vector3 } from 'three';
export interface MovementConfig {
    acceleration: number;
    deceleration: number;
    airDragFactor: number;
    maxWalkSpeed: number;
}
/**
 * Handles movement input processing and velocity management
 * Reuses temporary vectors for performance
 */
export declare class MovementController {
    private config;
    private targetVelocity;
    private velocityOnPlane;
    private deltaVelocity;
    private upAxis;
    constructor(config: MovementConfig);
    /**
     * Handle movement input and apply acceleration/deceleration
     * Modifies playerVelocity in-place for performance
     */
    handleMovementInput(movementVector: Vector3, playerVelocity: Vector3, isGrounded: boolean, delta: number): void;
    updateConfig(config: Partial<MovementConfig>): void;
}
//# sourceMappingURL=movement-controller.d.ts.map