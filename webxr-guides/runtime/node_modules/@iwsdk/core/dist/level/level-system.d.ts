/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/index.js';
declare const LevelSystem_base: import("elics").SystemConstructor<{
    /** Attach a default gradient dome on level roots when none provided. */
    defaultLighting: {
        type: Types.Boolean;
        default: true;
    };
}, {
    levelEntities: {
        required: import("elics").Component<{
            id: {
                type: Types.String;
                default: string;
            };
        }>[];
    };
}, import("../ecs/world.js").World, import("../ecs/system.js").System<{
    /** Attach a default gradient dome on level roots when none provided. */
    defaultLighting: {
        type: Types.Boolean;
        default: true;
    };
}, {
    levelEntities: {
        required: import("elics").Component<{
            id: {
                type: Types.String;
                default: string;
            };
        }>[];
    };
}>>;
/**
 * Manages the active level root, enforces identity transforms, and loads new levels on request.
 *
 * @remarks
 * - Destroys all {@link LevelTag}-tagged entities on level change.
 * - Loads GLXF via {@link GLXFImporter} when a URL is requested through {@link World.loadLevel}.
 * @category Scene
 */
export declare class LevelSystem extends LevelSystem_base {
    private loading;
    init(): void;
    update(): void;
    private startLevelChange;
}
export {};
//# sourceMappingURL=level-system.d.ts.map