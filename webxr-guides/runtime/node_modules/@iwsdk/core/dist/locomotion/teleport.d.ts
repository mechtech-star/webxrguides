/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/index.js';
declare const TeleportSystem_base: import("elics").SystemConstructor<{
    /** Downward acceleration for the parabolic guide (negative). */
    rayGravity: {
        type: Types.Float32;
        default: number;
    };
    /** Locomotor engine used for hit testing and teleport execution. */
    locomotor: {
        type: Types.Object;
        default: undefined;
    };
    /** Enable hand‑tracking micro‑gesture flow for activation/confirm. */
    microGestureControlsEnabled: {
        type: Types.Boolean;
        default: false;
    };
}, {}, import("../ecs/world.js").World, import("../ecs/system.js").System<{
    /** Downward acceleration for the parabolic guide (negative). */
    rayGravity: {
        type: Types.Float32;
        default: number;
    };
    /** Locomotor engine used for hit testing and teleport execution. */
    locomotor: {
        type: Types.Object;
        default: undefined;
    };
    /** Enable hand‑tracking micro‑gesture flow for activation/confirm. */
    microGestureControlsEnabled: {
        type: Types.Boolean;
        default: false;
    };
}, {}>>;
/**
 * Arc‑based teleportation with surface validation and hand/controller input.
 *
 * @remarks
 * - Uses locomotor ray hit tests and only allows targets with upward normals.
 * - In hand‑tracking mode, optional micro‑gestures can control activation
 *   (e.g., thumb tap to confirm).
 * - Draws a curved parabolic guide and landing marker with visual feedback.
 *
 * @category Locomotion
 */
export declare class TeleportSystem extends TeleportSystem_base {
    private rayOrigin;
    private rayDirection;
    private markers;
    private targetValid;
    private teleportMarker;
    private markerMat;
    private line;
    private teleportWasActive;
    private activeInput;
    private locomotor;
    init(): void;
    destroy(): void;
    update(): void;
}
export {};
//# sourceMappingURL=teleport.d.ts.map