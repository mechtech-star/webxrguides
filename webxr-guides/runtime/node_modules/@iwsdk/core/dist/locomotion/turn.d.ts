/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/component.js';
export declare enum TurningMethod {
    SnapTurn = 1,
    SmoothTurn = 2
}
declare const TurnSystem_base: import("elics").SystemConstructor<{
    /** Choose snap or smooth turning. */
    turningMethod: {
        type: Types.Int8;
        default: TurningMethod;
    };
    /** Degrees to rotate per snap. */
    turningAngle: {
        type: Types.Float32;
        default: number;
    };
    /** Degrees per second when smooth turning. */
    turningSpeed: {
        type: Types.Float32;
        default: number;
    };
    /** Enable swipe‑gesture turning in hand‑tracking mode. */
    microGestureControlsEnabled: {
        type: Types.Boolean;
        default: false;
    };
}, {}, import("../index.js").World, import("../ecs/system.js").System<{
    /** Choose snap or smooth turning. */
    turningMethod: {
        type: Types.Int8;
        default: TurningMethod;
    };
    /** Degrees to rotate per snap. */
    turningAngle: {
        type: Types.Float32;
        default: number;
    };
    /** Degrees per second when smooth turning. */
    turningSpeed: {
        type: Types.Float32;
        default: number;
    };
    /** Enable swipe‑gesture turning in hand‑tracking mode. */
    microGestureControlsEnabled: {
        type: Types.Boolean;
        default: false;
    };
}, {}>>;
/**
 * Player yaw rotation via snap or smooth turning.
 *
 * @remarks
 * - When snap turning is active, visual turn signals render on the right ray
 *   and respond to thumbstick left/right or hand micro‑gestures (if enabled).
 * - Smooth turning applies a continuous yaw at `turningSpeed` degrees/second.
 *
 * @category Locomotion
 */
export declare class TurnSystem extends TurnSystem_base {
    private turnSignals;
    private leftSignal;
    private rightSignal;
    private playerHeadPosition;
    init(): void;
    destroy(): void;
    update(delta: number): void;
    private updateSmooth;
    private updateSnap;
}
export {};
//# sourceMappingURL=turn.d.ts.map