/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/index.js';
/**
 * Procedural gradient background dome.
 *
 * @remarks
 * - Renders an inward‑facing sphere and writes to `scene.background`.
 * - Colors are Unity‑style sky/equator/ground; intensity multiplies shader output.
 *
 * @category Environment & Lighting
 */
export declare const DomeGradient: import("elics").Component<{
    sky: {
        type: Types.Color;
        default: [number, number, number, number];
    };
    equator: {
        type: Types.Color;
        default: [number, number, number, number];
    };
    ground: {
        type: Types.Color;
        default: [number, number, number, number];
    };
    intensity: {
        type: Types.Float32;
        default: number;
    };
    _needsUpdate: {
        type: Types.Boolean;
        default: true;
    };
}>;
//# sourceMappingURL=dome-gradient.d.ts.map