/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/index.js';
/**
 * Image-based lighting from a procedural gradient (PMREM of a gradient scene).
 *
 * @remarks
 * - Only affects `scene.environment`.
 *
 * @category Environment & Lighting
 */
export declare const IBLGradient: import("elics").Component<{
    sky: {
        type: Types.Color;
        default: [number, number, number, number];
    };
    equator: {
        type: Types.Color;
        default: [number, number, number, number];
    };
    ground: {
        type: Types.Color;
        default: [number, number, number, number];
    };
    intensity: {
        type: Types.Float32;
        default: number;
    };
    _needsUpdate: {
        type: Types.Boolean;
        default: true;
    };
}>;
//# sourceMappingURL=ibl-gradient.d.ts.map