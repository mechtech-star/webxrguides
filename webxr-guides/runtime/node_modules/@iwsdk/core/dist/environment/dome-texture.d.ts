/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/index.js';
/**
 * Background dome driven by a single equirectangular texture.
 *
 * @remarks
 * - `src` may be HDR/EXR or LDR (png/jpg/ktx2). Mapping is set to equirect when needed.
 * - Affects only `scene.background` and optional `scene.backgroundRotation`/`backgroundIntensity`.
 *
 * @category Environment & Lighting
 */
export declare const DomeTexture: import("elics").Component<{
    src: {
        type: Types.String;
        default: string;
    };
    blurriness: {
        type: Types.Float32;
        default: number;
    };
    intensity: {
        type: Types.Float32;
        default: number;
    };
    rotation: {
        type: Types.Vec3;
        default: [number, number, number];
    };
    _needsUpdate: {
        type: Types.Boolean;
        default: true;
    };
}>;
//# sourceMappingURL=dome-texture.d.ts.map