/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
import { Types } from '../ecs/index.js';
/**
 * Image‑based lighting from a texture or the built‑in Room environment.
 *
 * @remarks
 * - `src`: "room" | HDR/EXR equirect | LDR equirect. PMREM processed to `scene.environment`.
 * - `rotation` controls `scene.environmentRotation` in radians.
 * - Does not set `scene.background`.
 *
 * @category Environment & Lighting
 */
export declare const IBLTexture: import("elics").Component<{
    src: {
        type: Types.String;
        default: string;
    };
    intensity: {
        type: Types.Float32;
        default: number;
    };
    rotation: {
        type: Types.Vec3;
        default: [number, number, number];
    };
    _needsUpdate: {
        type: Types.Boolean;
        default: true;
    };
}>;
//# sourceMappingURL=ibl-texture.d.ts.map